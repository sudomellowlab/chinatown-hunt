# Build output — produced by `node build.js`, and by CI as a downloadable artifact
dist/

# Dependencies and Playwright output
node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# OS files
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/
.directory
*~

# .claude/ is committed on purpose (shared preview config). Keep secrets out of it;
# personal overrides go in settings.local.json, which is ignored.
.claude/settings.local.json

# Recorded walks: timestamped GPS tracks. The repo is public, so keep them out of git.
walks/
