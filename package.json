{
  "name": "chinatown-hunt",
  "private": true,
  "type": "module",
  "description": "GPS treasure hunt for Telok Ayer and Ann Siang Hill. The build and unit tests need no install; npm is only for Playwright.",
  "scripts": {
    "build": "node build.js",
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
